commands:
  onair:
    description: "bangumi onair"
  onair.day:
    description: "bangumi onair today"
  onair.season:
    description: "bangumi onair this season"
  onair.update:
    description: "update bangumi database"
    messages:
      updating: "updating bangumi database…"
      updated: "bangumi database updated"
      failed: "update failed"
  onair.drop:
    description: "drop bangumi database"
    messages:
      failed: "drop failed"
      dropped: "dropped"

_config:
  excludeOld: exclude old bangumi
  showChineseTitle: show Chinese title if available
  separateWeekdays: separate onair.season message by weekdays, to avoid long message constraint
